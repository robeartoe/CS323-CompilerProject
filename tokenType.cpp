// Don't think this will be used. Since we can put them inside the class token.

// Contains enum for the various token types:

enum tokenType {
  // Single Character Tokens:

  //One or Two Char Tokens:

  //Literals

  //Keywords:

 };
